const names = ["Fajar", "Aji", "Pamungkas"];

for (const name of names) {
  document.writeln(`<p>${name}</p>`);
}

const fullName = "Fajar Aji Pamungkas";
for (const character of fullName) {
  document.writeln(`<p>${character}</p>`);
}

//Object tidak bisa for Of
